# resources/

Per-week reading / resource pages, authored as `NN_resources.qmd` (e.g. `02_resources.qmd`)
and linked from the "Resources" column of `schedule.qmd`. One page per week; keep them short —
they point at readings and external links, not full content.
